export enum EnJob {
  SCHEDULED_TX = 'scheduled_tx',
  PUSH_NOTIFICATION = 'push_notification',
}

export enum EnQueue {
  SCHEDULED_TX = 'scheduled_tx_queue',
  PUSH_NOTIFICATION = 'push_notification',
}
